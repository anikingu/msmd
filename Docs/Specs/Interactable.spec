Interactable := {
    // xpath starting from the root node of the webpage
    full_xpath: (string),
    // xpath starting from the nearest node with an id
    relative_xpath: (string),
    // attributes of the node
    attributes: (string)
}